- Criar uma lista;

- Criar variavel posição = 0;

- Botão inserir:
   - Inicializar classe ator;
   - Set todas as variaveis;
                //ator.setNome(jTextField1.getText()); 
                    > Pego o texto (getText) do jTextField1 e seto no ator;
                //ator.setSalario(Double.parseDouble(jTextField6.getText()));       
                //ator.setEstado(jComboBox1.getSelectedItem().toString());
   - Adicionar na lista;

- Botão limpar;

- Botão exibir status;
   - Pegar as informações;
                //Ator ator = lista.get(posicao);
   - Mostrar o resultado do método;
                //JOptionPane.showMessageDialog(null, " O status do ator é:" + ator.returnStatus());

- Botão exibir ganhos;
   - Mesma coisa q o status;

- Botão primeiro:
   - Desativar botão anterior;
                 //btAnterior.setEnabled(false);
   - Se a lista é maior q 0, então ela tem algum elemento pra mostrar;
                 //if (lista.size() > 0)
 - Pegar elemento da lista; 
                 //Ator elemento = lista.get(0);
   - Mostrar as informações do ator;
                //jTextField1.setText(elemento.getNome());
                      > Faço o oposto do que eu fiz no Inserir:
                      > Pego o nome (getNome) do elemento e seto no jTextField1 pra mostrar;
- Se a lista tiver só um elemento, desabilitar proximo e anterior;

- Botão anterior:
  - Todos os botões ativados;
   - Pegar o elemento anterior da lista;
                // Ator elemento = lista.get(posicao - 1);

- Botão próximo:
    - Todos os botões ativados;
    - Pegar o elemento seguinte da lista;
                // Ator elemento = lista.get(posicao + 1);
    - Se eu estiver na última posição, desativar próximo e último;
                //if (lista.size() - 1 == posicao)
         
- Botão último:
     - Desativar botões proximo e ultimo;
     - Pegar o último botão da lista;
                //Ator elemento = lista.get(lista.size() - 1);

   